## taro 小程序项目模板

```bash
pnpm install
pnpm build:weapp
```

常用命令：

- `pnpm lint`：检查 TS/JS 代码
- `pnpm lint:fix`：自动修复 TS/JS 代码问题
- `pnpm lint:style`：检查样式文件
- `pnpm lint:style:fix`：自动修复样式问题
- `pnpm format`：格式化项目文件
- `pnpm format:check`：检查格式是否符合规范
